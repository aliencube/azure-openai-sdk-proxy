@page "/tests"
@rendermode InteractiveServer

<DebugTargetComponent OnChange="OnValueChanged" />
<DebugButtonComponent Input="@currentValue" />

@code {
    private object? currentValue;

    private void OnValueChanged(int newValue)
    {
        currentValue = newValue;
    }
}